<header class="header">
	<div>
		{{> logo}}
	</div>

	<div class="header_nav">
		<div class="header_nav_right">
			{{>nav list=header}}
		</div>
	</div>
</header>

{{#with main}}
<div class="hero">
	<img src="{{image}}" alt="Hero image" class="hero_image">
	<div class="hero_content">
		<h1 class="hero_title">{{heading}}</h1>
		<p class="hero_text">{{descriotion}}</p>
		<a href="{{href}}" class="button card_button">{{button}}</a>
	</div>
</div>
{{/with}}

<main class="blog">
	{{#each featured}}
	<div class="card">
		<div class="card_image" style="background-image: url({{photo}})"></div>
		<h2 class="card_title">{{heading}}</h2>
		<p class="card_text">{{info}}</p>
		<a href="{{href}}" class="blog_button">{{linkText}}</a>
	</div>
	{{/each}}
</main>

<div class="form">
	{{#each actions}}
	<div class="form_content">
		<h3 class="form_title">{{heading}}</h3>
		<a class="button form_button" href="{{url}}">{{text}}</a>
	</div>
	<div class="form_action"></div>
	{{/each}}
</div>

<footer class="footer">
	<div class="col-3">
		{{> logo}}
		<span class="footer_copyright">Copyright 2023-2024</span>
	</div>

	{{#each footer}}
	<div>
		<span class="footer_title">{{title}}</span>
		{{> nav list=links isVertical=true}}
	</div>
	{{/each}}
</footer>